const CONFIG = {
  BASE_URL: 'https://restaurant-api.dicoding.dev',
  PICTURE: 'https://restaurant-api.dicoding.dev/images',
  DATABASE_NAME: 'pesanfood-db',
  DATABASE_VERSION: 1,
  OBJECT_STORE_NAME: 'favorite',
  // CACHE_NAME: `PesanFood-${new Date().getTime()}`, // For development
  CACHE_NAME: 'PesanFood-cache', // For production
};

export default CONFIG;